/**
 * Settings Screen (Tab)
 * Wrapper to display settings content in main tab navigation
 */

import { useRouter } from 'expo-router';
import SettingsScreen from '../(settings)/index';

export default function SettingsTabScreen() {
  return <SettingsScreen />;
}
